---
description: Set up (or restructure) a research repository — per-author folders, portable paths, one canonical dataset, provenance, a round lock, and a generated CLAUDE.md.
argument-hint: "[optional: project name]"
---

Invoke the `research-setup` skill and follow it.

If "$ARGUMENTS" is non-empty, treat it as the project name and confirm the slug
and env prefix it produces before going on. Otherwise start the interview from
the beginning.

Interview first. Do not create anything until you know whether the project is
empirical, who is working on it, and where the data lives.
