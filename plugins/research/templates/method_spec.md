# Method spec — @@PROJECT@@

The single implementable specification for the current round, frozen before any
implementer runs. Both builds implement THIS.

Rewritten each round that turns on a number. Rounds that do not can leave it.

## The quantity, and the comparison that decides the round

## Sample and screens

## Variable definitions, with timing and lags

## Estimator, and every baseline's exact construction

## Cross-validation scheme

Name it explicitly — grouped by the panel unit, never a random split on a panel.

## Winsorization

## Evaluation frame and metric

## Inference

## Open choices

Anything not pinned above. An implementer who hits one must stop and ask, not
guess — and the answer comes back here before either build re-runs.
